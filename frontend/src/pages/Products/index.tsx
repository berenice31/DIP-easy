import { Box, Heading } from "@chakra-ui/react";

const Products = () => {
  return (
    <Box>
      <Heading mb={6}>Produits</Heading>
      {/* Contenu à implémenter */}
    </Box>
  );
};

export default Products;
